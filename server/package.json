{
  "name": "server",
  "version": "1.0.0",
  "description": "Server to serve the journal react app",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Alex Chung",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "mongoose": "^8.5.1"
  }
}
